<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <link
      href="https://fonts.googleapis.com/css?family=Inter"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
    <title><%= diaryContent[0].title %></title>
  </head>
  <body class="singleBlog_body">
    <h1><%= diaryContent[0].title %></h1>
    <div class="singleBlog_navbar">
      <a href="/blog">Blog</a>
      <i
        class="fa fa-chevron-right fa-2xs"
        style="font-size: 10px; margin-top: 10px"
      ></i>
      <a><%= diaryContent[0].title %></a>
    </div>
    <div class="singleBlog_contents">
      <% if (image) { %> <% image = image.toString("base64"); %>
      <img src="data:image/jpg;base64,<%= image %>" class="single_image" />
      <% } else { %>
      <img src="/images/missing_img.jpg" class="single_image" />
      <% } %>
      <p><%= diaryContent[0].content_desc %></p>
    </div>
  </body>
</html>
